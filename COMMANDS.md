# 라인 웹사이트 — 작업 명령어 모음

## 1. 시작 (터미널)

```bash
cd laain-site
git init
git add .
git commit -m "init: 라인 홈+사업소개 목업"
claude
```

## 2. Claude Code 안에서 (한 번에 하나씩)

**① 현황 파악**
> 이 폴더의 index.html과 data.js를 읽고 현재 상태를 요약해줘.

**② 이력 분류 검토**
> data.js의 6개 영역 분류 중 어색하게 들어간 항목이 있는지 검토하고, 옮기면 좋을 것들을 알려줘. 바로 고치지 말고 먼저 보여줘.

**③ 홈 / 사업소개 분리**
> 지금 한 페이지인 걸 홈(index.html)과 사업소개(business.html)로 나눠줘. 홈은 각 섹션 요약 + 대표사례 맛보기, 사업소개는 지금의 2층 구조 풀버전으로. 헤더·푸터·색상·폰트는 공유하고, 전체 재작성 말고 기존 구조를 살려서 해줘.

**④ 프로젝트 아카이브**
> data.js를 활용해서 프로젝트 아카이브 페이지(projects.html)를 만들어줘. 전문분야·연도·발주처로 필터되는 카드 그리드로. 디자인 토큰은 기존 그대로.

**⑤ 소개·문의 페이지**
> 소개 페이지(미션·핵심가치 공감/최적화/지속성·팀 소개)와 문의 페이지를 만들어줘. 회사소개서 내용 기반으로, 기존 디자인 톤 유지해서.

**⑥ 로고 교체 (SVG 준비되면)**
> 헤더 로고를 고해상도 SVG로 교체할 거야. 파일 첨부할게.

**⑦ 마무리 점검**
> 모바일·태블릿에서 깨지는 부분 점검하고 고쳐줘.

## 3. 작업 팁

- 큰 변경 전 저장점: `git commit -am "메모"` (문제 시 되돌리기)
- "전체 다시 쓰지 말고 해당 부분만 고쳐줘" 붙이기
- "먼저 보여주고 확인받아" 라고 하면 검토 후 진행

## 4. 로컬 미리보기 / 배포

로컬:
```bash
python -m http.server 8000
```
→ 브라우저 `localhost:8000`

배포:
```bash
npm i -g vercel
vercel
```
또는 netlify.com 에 폴더 드래그&드롭

## 5. 자주 쓰는 Git

```bash
git status              # 바뀐 파일 확인
git add .               # 변경 담기
git commit -m "메모"     # 저장점 만들기
git log --oneline       # 저장점 목록
git checkout <id> -- .  # 특정 저장점으로 되돌리기
```
